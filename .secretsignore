# ripsecrets allowlist — paths here are NOT scanned for secrets.
# Keep this minimal; prefer avoiding literal high-entropy strings.

# Test ed25519 keypair for airgapped VM tests (lib/jwt-keys.nix).
# Seed is 0x42 × 32 (obviously not secret); pubkey is PUBLIC by
# definition. Both are TEST-ONLY — the airgapped k3s-full fixture
# never touches production. ripsecrets flags any 40+char high-entropy
# base64 literal; no inline ignore is supported.
nix/tests/lib/jwt-keys.nix
