{
 "_comment": "helm-env-schema-parity allowlist (merged_bug_161 class close): every entry is a config-schema field DELIBERATELY not rendered as a chart env, with the why. The check fails when a schema field is neither rendered nor listed here \u2014 a knob added in Rust but forgotten in the chart (the RIO_LOG_RETENTION_DAYS / RIO_LOG_CORS_ALLOW_ORIGINS class) cannot ship silently again. rio-builder has no chart Deployment (builder env comes from the controller's SpawnIntent Job templates) and is out of scope.",
 "rio-scheduler": {
  "RIO_ALLOW_REFERENCE_CHANGE": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_ESTABLISHMENT_REPORT_SLACK_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_HMAC_KEY_PATH": "conditional wiring: rendered only when its feature flag/secret mount is enabled (rio.mounts include); absent at chart defaults by design.",
  "RIO_JWT__KEY_PATH": "conditional wiring: rendered only when its feature flag/secret mount is enabled (rio.mounts include); absent at chart defaults by design.",
  "RIO_JWT__REQUIRED": "conditional wiring: rendered only when its feature flag/secret mount is enabled (rio.mounts include); absent at chart defaults by design.",
  "RIO_MATERIALIZATION__ATTEMPT_DEADLINE_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_MATERIALIZATION__CONVERSION_MIN_PARK_DWELL_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_MATERIALIZATION__CONVERSION_REQUIRES_WORKER_CHARGE": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_MATERIALIZATION__MAX_ATTEMPTS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_MATERIALIZATION__PARK_BACKOFF_BASE_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_MATERIALIZATION__PARK_BACKOFF_CAP_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_METRICS_ADDR": "the port/address is baked into the Service + probe templates; changing it is a coupled chart+code change, not an operator knob.",
  "RIO_POISON__REQUIRE_DISTINCT_WORKERS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_POISON__THRESHOLD": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RETRY__BACKOFF_BASE_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RETRY__BACKOFF_MAX_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RETRY__BACKOFF_MULTIPLIER": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RETRY__INFRA_RETRY_WINDOW_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RETRY__JITTER_FRACTION": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RETRY__MAX_EXEMPT_INFRA_RETRIES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RETRY__MAX_INFRA_RETRIES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RETRY__MAX_RETRIES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RETRY__MAX_TIMEOUT_RETRIES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__CLUSTER": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__DEFAULT_DISK": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__DEFAULT_TIER": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__FEATURE_PROBES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__HW_BENCH_MEM_FLOOR": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__HW_CLASSES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__HW_COST_SOURCE": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__HW_COST_TOLERANCE": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__HW_EXPLORE_EPSILON": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__LADDER_BUDGET": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__LEAD_TIME_SEED": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__MAX_CONSOLIDATION_TIME": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__MAX_CORES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__MAX_DISK": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__MAX_FLEET_CORES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__MAX_FORECAST_CORES_PER_TENANT": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__MAX_KEYS_PER_TENANT": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__MAX_LEAD_TIME": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__MAX_MEM": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__MAX_NODE_CLAIMS_PER_CELL_PER_TICK": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__METAL_SIZES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__PROBE": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__REFERENCE_HW_CLASS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__RING_BUFFER": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__SEED_CORPUS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SLA__TIERS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SOFT_FEATURES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_STORE__BALANCE_HOST": "unwired by default: this component dials the single Service endpoint (RIO_*__ADDR); the per-replica balance pair is wired only where a component actually balances. The parity check forces the conversation the day that changes.",
  "RIO_STORE__BALANCE_PORT": "unwired by default: this component dials the single Service endpoint (RIO_*__ADDR); the per-replica balance pair is wired only where a component actually balances. The parity check forces the conversation the day that changes.",
  "RIO_TICK_INTERVAL_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value."
 },
 "rio-store": {
  "RIO_LOG_BYTES_BUDGET": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_LOG_CUT_INTERVAL_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_LOG_CUT_THRESHOLD_BYTES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_LOG_INGEST_BYTE_CAP": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_LOG_MAX_CHUNKS_PER_EXEC": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_LOG_MAX_STREAMS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NAR_BUFFER_BUDGET_BYTES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_CHUNK_CACHE_CAPACITY_BYTES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SUBSTITUTE_ADMISSION_PERMITS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SUBSTITUTE_STALL_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_SERVICE_BYPASS_CALLERS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_MATERIALIZATION__POLL_INTERVAL_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_JWT__KEY_PATH": "conditional wiring: rendered only when its feature flag/secret mount is enabled (rio.mounts include); absent at chart defaults by design.",
  "RIO_JWT__REQUIRED": "conditional wiring: rendered only when its feature flag/secret mount is enabled (rio.mounts include); absent at chart defaults by design.",
  "RIO_HMAC_KEY_PATH": "conditional wiring: rendered only when its feature flag/secret mount is enabled (rio.mounts include); absent at chart defaults by design.",
  "RIO_SIGNING_KEY_PATH": "conditional wiring: rendered only when its feature flag/secret mount is enabled (rio.mounts include); absent at chart defaults by design.",
  "RIO_METRICS_ADDR": "the port/address is baked into the Service + probe templates; changing it is a coupled chart+code change, not an operator knob."
 },
 "rio-gateway": {
  "RIO_MAX_CHANNELS_PER_CONNECTION": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_MAX_CONNECTIONS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_MAX_SESSIONS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RATE_LIMIT": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_RESOLVE_TIMEOUT_MS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_JWT__KEY_PATH": "conditional wiring: rendered only when its feature flag/secret mount is enabled (rio.mounts include); absent at chart defaults by design.",
  "RIO_JWT__REQUIRED": "conditional wiring: rendered only when its feature flag/secret mount is enabled (rio.mounts include); absent at chart defaults by design.",
  "RIO_METRICS_ADDR": "the port/address is baked into the Service + probe templates; changing it is a coupled chart+code change, not an operator knob.",
  "RIO_HEALTH_ADDR": "the port/address is baked into the Service + probe templates; changing it is a coupled chart+code change, not an operator knob.",
  "RIO_STORE__BALANCE_HOST": "unwired by default: this component dials the single Service endpoint (RIO_*__ADDR); the per-replica balance pair is wired only where a component actually balances. The parity check forces the conversation the day that changes.",
  "RIO_STORE__BALANCE_PORT": "unwired by default: this component dials the single Service endpoint (RIO_*__ADDR); the per-replica balance pair is wired only where a component actually balances. The parity check forces the conversation the day that changes."
 },
 "rio-controller": {
  "RIO_NODECLAIM_POOL__DEFAULT_LEAD_TIME_SEED": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__FETCHER_FUSE_CACHE_BYTES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__FUSE_CACHE_BYTES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__KUBE_BUILD_SCHEDULER_ENABLED": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__LEAD_TIME_SEED": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__LEASE_NAME": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__MAX_CONSOLIDATION_TIME": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__MAX_FLEET_CORES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__MAX_LEAD_TIME": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__MAX_NODE_CLAIMS_PER_CELL_PER_TICK": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__MAX_NODE_DISK": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__METAL_SIZES": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__MIN_CONSOLIDATION_TIME": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__REFERENCE_HW_CLASS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_NODECLAIM_POOL__SKETCH_HALFLIFE_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_GC_INTERVAL_HOURS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_DRAIN_GRACE_SECS": "deliberate: the binary default is the supported production value; the override path is the component's extraEnv/--set (an explicit operator act). The parity check forces this entry to be revisited if the knob is ever meant to be a first-class chart value.",
  "RIO_METRICS_ADDR": "the port/address is baked into the Service + probe templates; changing it is a coupled chart+code change, not an operator knob.",
  "RIO_HEALTH_ADDR": "the port/address is baked into the Service + probe templates; changing it is a coupled chart+code change, not an operator knob."
 }
}
