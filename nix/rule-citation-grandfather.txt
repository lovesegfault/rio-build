fuzz/rio-nix/fuzz_targets/nar_parsing.rs	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
infra/helm/rio-build/templates/gateway-scaledobject.yaml	stale rule citation `gw.conn.session-cap` — the spec defines `gw.conn.session-cap+2`; re-derive the cite against the current rule text and re-stamp
infra/helm/rio-build/templates/rbac.yaml	stale rule citation `ctrl.ephemeral.reap-excess-pending` — the spec defines `ctrl.ephemeral.reap-excess-pending+4`; re-derive the cite against the current rule text and re-stamp
infra/helm/rio-build/values.yaml	stale rule citation `store.substitute.admission` — the spec defines `store.substitute.admission+2`; re-derive the cite against the current rule text and re-stamp
infra/helm/rio-build/values.yaml	stale rule citation `store.substitute.stall-abort` — the spec defines `store.substitute.stall-abort+2`; re-derive the cite against the current rule text and re-stamp
nix/cilium-render.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/misc-checks.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/node-ami.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/default.nix	stale rule citation `builder.shutdown.sigint+4` — the spec defines `builder.shutdown.sigint+5`; re-derive the cite against the current rule text and re-stamp
nix/tests/fixtures/k3s-full.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/fixtures/kwok.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/fixtures/standalone.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/helm/06-jwt-mount.sh	stale rule citation `sec.jwt.pubkey-mount` — the spec defines `sec.jwt.pubkey-mount+2`; re-derive the cite against the current rule text and re-stamp
nix/tests/lib/hmac-keys.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/ca-cutoff.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/cli.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/dashboard-gateway.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/dashboard.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/fetcher-split.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/forecast-provisioning.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/lifecycle.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/lifecycle/bootstrap-job-ran.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/lifecycle/bootstrap-tenant.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/lifecycle/health-shared.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/lifecycle/jwt-mount-present.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/lifecycle/materialization-boundary.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/lifecycle/store-rollout.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/netpol.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/observability.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/protocol.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/security/standalone.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/substitute-scale.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
nix/tests/scenarios/substitute.nix	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
rio-cli/tests/parse.rs	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
rio-dashboard/vite.config.ts	r[...] marker outside tracey enrollment — .config/tracey/config.styx does not include this file, so the marker is validated by NOTHING; enroll the file (include/test_include) or rewrite the marker as prose
rio-migrations/src/migrations.rs	stale rule citation `sched.merge.exec-correlation+7` — the spec defines `sched.merge.exec-correlation+8`; re-derive the cite against the current rule text and re-stamp
