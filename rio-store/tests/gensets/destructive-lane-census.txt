# [GEN-SET] destructive-lane census (W10-D family, merged_bug_050).
# Generator: rio-store/src/gc/lane.rs census::destructive_lane_census —
# scan over the spawn-periodic FAMILY (spawn_periodic +
# spawn_periodic_with call sites, production half, whole-crate
# embedded universe) x the named REFUSAL PREDICATE
# (reaches-delete-sink ∧ NOT-DestructiveLane-routed; sinks =
# DELETE FROM / delete_by_key / INSERT INTO pending_s3_deletes,
# transitively over fn bodies). Lane set = this output ∪ {run_gc}
# (PINNED: RPC-spawned per TriggerGC — asserted structurally in the
# same test: hold::gate mint + clearance threaded to collect_cycle).
# Format: lane \t file \t routed \t destructive (sorted).
# routed=false destructive=false rows are the lawful non-destructive
# controls (gauge publishers) — they prove the scan is not vacuous.
gc-collect-backstop	gc/collect.rs	routed=true	destructive=true
gc-drain-task	gc/drain.rs	routed=true	destructive=true
gc-gauge-publisher	gc/state.rs	routed=false	destructive=false
gc-orphan-scanner	gc/orphan.rs	routed=true	destructive=true
log-ttl-sweep	logs/sweep.rs	routed=true	destructive=true
store-gauges	grpc/admin.rs	routed=false	destructive=false
