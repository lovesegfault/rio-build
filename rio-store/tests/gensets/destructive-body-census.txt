# [GEN-SET] destructive-body census (bug_084/merged_bug_006, R31).
# Derived by gc/lane.rs::census::derive_destructive_bodies over the
# whole-crate CENSUS_SOURCES corpus (minus the typed test-plane
# exemptions): every production loop that transitively reaches a
# durable gc-victim sink, with its demand form (in-loop =
# authorize_batch at the loop boundary; via:<callees> = every sink
# path runs through self-authorizing callees, e.g. the consulted
# placeholder reap). Regenerate by running destructive_body_census
# and pasting the failure output's derived rows.
backend/mod.rs	exists_batch	via:run
gc/collect.rs	collect_cycle	in-loop
gc/collect.rs	run_post_drain_tail	in-loop
gc/drain.rs	drain_once	in-loop
gc/orphan.rs	scan_once	in-loop
gc/sweep.rs	sweep	in-loop
gc/sweep.rs	sweep	via:drop
grpc/admin.rs	verify_chunks	via:exists_batch
grpc/put_path/common.rs	wait_for_concurrent_upload	via:claim_placeholder
grpc/put_path_batch.rs	commit_batch	via:drop
grpc/put_path_batch.rs	put_path_batch_impl	via:claim_placeholder,stage_nar_for_batch
grpc/put_path_chunked/mod.rs	claim_ca_placeholders	via:claim_placeholder
grpc/put_path_chunked/mod.rs	commit_chunked	via:drop
grpc/put_path_chunked/mod.rs	put_path_chunked_impl	via:claim_placeholder,wait_for_concurrent_upload
logs/service.rs	cut_while_due	via:do_cut
logs/service.rs	drain	via:cut_bounded
logs/service.rs	drive	via:cut_once_if_nonempty,on_inbound
logs/service.rs	drive	via:on_inbound
logs/sweep.rs	sweep_expired_logs	in-loop
materialize/executor.rs	acquire_baseline	via:drop
materialize/executor.rs	execute_job_inner	via:acquire_baseline,drain,drop,resolve_path
materialize/executor.rs	execute_job_inner	via:acquire_baseline,resolve_path
materialize/executor.rs	resolve_path	via:try_substitute_subscribed
materialize/mod.rs	spawn_materialization_executor	via:worker_loop
materialize/mod.rs	spawn_with_transports	via:worker_loop
materialize/mod.rs	worker_loop	via:drop,execute_job_with_progress
substitute.rs	do_substitute	via:try_upstream
substitute.rs	try_substitute_subscribed	via:try_substitute_inner
