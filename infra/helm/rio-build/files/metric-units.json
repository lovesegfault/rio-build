{
  "_comment": "Unit registry for KEDA prometheus triggers (bug_299). rio.promTrigger (templates/_helpers.tpl) joins a metric and a threshold knob through this file and FAILS THE RENDER when either name is missing or their units disagree — a paths-knob dividing a jobs-gauge cannot ship again. Add a row when adding a trigger; units are free-form labels, equality-checked only.",
  "metrics": {
    "rio_scheduler_substituting_derivations": "jobs",
    "rio_scheduler_open_attempts": "attempts",
    "rio_gateway_channels_active": "sessions"
  },
  "knobs": {
    "targetBacklogJobsPerReplica": "jobs",
    "targetBuildersPerReplica": "attempts",
    "targetSessionsPerPod": "sessions"
  }
}
