[
  {
    "_comment": "Dashboard east-west upstream registry (bug_238/bug_403): ONE record per backend the dashboard nginx dials. Three generated consumers — docker.nix renders the nginx upstream blocks (envsubst placeholders, env defaults below), networkpolicy.yaml ranges this file for the dashboard-egress AND the matching backend-ingress entries (an upstream cannot exist without its policy pair), and dashboard.yaml sets each env from .Values.namespaces so chart overrides reach the baked image. Add a backend HERE; the conf, both policy sides, and the env wiring follow.",
    "name": "rio_scheduler",
    "component": "rio-scheduler",
    "service": "rio-scheduler-leader",
    "namespaceRef": "system",
    "port": 9001,
    "env": "RIO_SCHEDULER_FQDN"
  },
  {
    "name": "rio_store",
    "component": "rio-store",
    "service": "rio-store",
    "namespaceRef": "store",
    "port": 9002,
    "env": "RIO_STORE_FQDN"
  }
]
