# timeout-Err census — GENERATED by tests/timeout_census.rs (scan section).
# Regenerate: cargo test -p rio-controller --test timeout_census -- --ignored regenerate_timeout_census
# Columns: <src-relative path>:<line>\t<class>
guard.rs:186	delay
guard.rs:513	refusal
reconcilers/componentscaler/mod.rs:272	delay
reconcilers/componentscaler/mod.rs:349	delay
reconcilers/componentscaler/mod.rs:97	delay
reconcilers/gc_schedule.rs:174	delay
reconcilers/gc_schedule.rs:215	delay
reconcilers/mod.rs:71	refusal
reconcilers/nodeclaim_pool/lifecycle_tests.rs:905	delay
reconcilers/nodeclaim_pool/mod.rs:1404	delay
# --- declared (cross-crate) rows below this line are NOT regenerated ---
# E5: the store envelope family (wave-8 landed) enrolled in the benign
# delay-only quadrant. Declared, not scanned: the per-crate build
# sandbox carries only this crate's sources; widening the scan tier is
# the round-9 tier-2 census plane's burn-down item (the scope-red
# corpus proves the generator is scope-capable today). Sentinel
# coverage notes: these holds are bounded by their own typed envelope
# plane (NarHoldEnvelope deadlines + the stall watchdog), not by the
# controller skew sentinel — expiry parks or releases, never commits.
rio-store/src/substitute.rs (NarHoldEnvelope deadline)	delay	hold released; claim re-claimable; envelope-bounded
rio-store/src/grpc/put_path/common.rs (BUDGET_WAIT_GRACE=180s shed)	delay	budget wait shed; upload retryable; envelope-bounded
rio-store/src/grpc/put_path/common.rs (NarHoldEnvelope::for_ingest_cap)	delay	ingest hold capped; envelope-bounded
rio-builder/src/runtime/setup.rs (COLD_START_CONNECT_BUDGET first-connect)	refusal	budget breach = exit nonzero; Job backoff/CrashLoopBackOff carry escalation; steady-state reconnect keeps the infinite posture (dual face); envelope-bounded (live_056-b)
